// A basic calculator

let pro = process.argv

for (let i = 2; i < pro.length; i++) {
  const element = pro[i];
  console.log(element);
}

const calculator = (input) => {
  for (let i = 0; i < pro.length; i++) {
    const element = pro[i];
    console.log(element);
  }
}

